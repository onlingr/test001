/**
 * AI 服務已移除
 * 此檔案保留結構以避免任何潛在的導入錯誤，但功能為空。
 */

export const generateMenuDescription = async (dishName: string, category: string): Promise<string> => {
  return "";
};

export const suggestPrice = async (dishName: string): Promise<number> => {
  return 0;
};